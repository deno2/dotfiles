[user]
    email = denesimpson@googlemail.com
    name = Dene Simpson

[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=local
    type = cat-file -t
    dump = cat-file -p

[credential]
    helper = osxkeychain

[github]
	user = deno2
